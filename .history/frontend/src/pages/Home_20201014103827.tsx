// @flow 
import { gql } from '@apollo/client';
import * as React from 'react';
type Props = {
    
};

const EXERCISES = gql`
query {
    ex
}
`

export const Home = (props: Props) => {
    return (
        <div>
            
        </div>
    );
};